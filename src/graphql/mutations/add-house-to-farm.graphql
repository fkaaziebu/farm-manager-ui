mutation AddHouseToFarm(
  $farmId: String!
  $houseNumber: String!
  $rooms: [CreateRoomInput!]!
) {
  addHouseToFarm(farmId: $farmId, houseNumber: $houseNumber, rooms: $rooms) {
    house_number
    id
    rooms {
      animals {
        id
        tag_number
      }
    }
    status
  }
}
