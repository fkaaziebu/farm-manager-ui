mutation CreateFarm(
  $name: String!
  $location: String!
  $area: String!
  $farmType: FarmType!
) {
  createFarm(
    name: $name
    location: $location
    area: $area
    farmType: $farmType
  ) {
    id
    name
  }
}
